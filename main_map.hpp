#include <map>
#include <string>

using namespace std;

    const map<string, string> ru_map = {
        { "Chance!", "Chance!" }
    };

    const map<string, string> en_map = {
        { "Chance!", "ШАНС!" }
    };
